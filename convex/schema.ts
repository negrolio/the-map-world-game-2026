import { defineSchema } from 'convex/server'

/**
 * Esquema vacío hasta la feature de caché persistente de adivinanzas.
 * Ver docs/tasks/backend-related-features/convex-setup/.
 */
export default defineSchema({})
